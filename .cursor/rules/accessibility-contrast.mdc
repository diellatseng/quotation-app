---
description: 使用者有視力／可見度問題 — 易讀性優先，灰色僅在必要時使用
alwaysApply: true
---

# 無障礙：易讀性與輸入簡易度

**本專案主要使用者有 visibility／視力問題，閱讀困難。** 所有 UI 變更必須**隨時**以易讀性為第一優先，不可為了視覺層次而犧牲文字清晰度。

## 灰色文字（`text-muted-foreground`）

**預設用 `text-foreground`。** 灰色僅在以下情況使用，且仍須符合 WCAG AA：

| 允許灰色 | 不允許灰色 |
|----------|------------|
| input `placeholder` | 標籤、欄位值、表格內容 |
| 真正次要的單行提示（如「顯示於 PDF」） | 金額、日期、客戶名、狀態說明 |
| 空狀態的補充一句話 | 表頭、Dialog 說明中的關鍵資訊 |
| 列印／PDF 頁尾頁碼 | 錯誤訊息、摘要統計 |

- **必讀內容**一律 `text-foreground`（必要時加 `font-medium` / `font-semibold`）。
- 不要在 `<TableHead>` 上覆寫成 `text-muted-foreground`。
- `DialogDescription` 若含階段名、金額等資訊 → 加 `className="text-foreground"`。
- 結構化表單固定字用 `structuredFixedTextClassName` 或 `text-foreground font-semibold`。

## 結構化輸入（減少打字）

當欄位有固定格式時，**必須拆成多個短輸入 + 固定文字**，使用者只填會變動的部分。**各欄位獨立存 state，不可每次 keystroke 把組合字串 parse 回其他欄位**（例如地號打 `-` 時不可污染段名）。

```tsx
// ❌ BAD — 必讀資訊用灰色
<p className="text-muted-foreground">{stage.stage_name} · {fmt(amount)}</p>

// ✅ GOOD
<p className="text-sm font-medium text-foreground">{stage.stage_name} · {fmt(amount)}</p>
```

### 規則

1. **固定文字不可編輯**：括號、`建築字第`、`號` 等用 `InputGroupText` 顯示。
2. **Placeholder 只示範最短變動值**：如 `961`、`123`。
3. **合理預填**：常用預設在空表單即顯示。
4. **數字欄**用 `inputMode="numeric"`。
5. **組合預覽**用 `text-foreground font-medium`。

## 表單尺寸

- 結構化短輸入：`min-h-10 text-base font-medium`（`structuredInputClassName`）。
- 欄位標籤：`text-base font-semibold text-foreground`。

## 主要按鈕（CTA）

- **下一步、儲存、送出** → `variant="default"`（`primary` 底 + 白字），對比須 ≥ 4.5:1。
- **儲存草稿、取消** → `variant="outline"` 或 `ghost`（`text-secondary`，勿用 `text-muted-foreground`）。
- **禁止**用 `variant="accent"`（粉紅底）承載主要 CTA — 白字對比不足；accent 僅供次要強調且用深色字。

## 修改 UI 前的檢查

1. 使用者必須清楚閱讀嗎？→ `text-foreground`。
2. 這段是裝飾性層次還是實際資訊？→ 實際資訊禁止灰色。
3. 能否用 select / 預填 / 數字框代替自由文字？→ 優先採用。
